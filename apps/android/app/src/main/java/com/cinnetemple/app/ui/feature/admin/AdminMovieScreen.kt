package com.cinnetemple.app.ui.feature.admin

import androidx.compose.runtime.Composable
import androidx.navigation.NavController
import com.cinnetemple.app.ui.components.PlaceholderScreen

/**
 * PLACEHOLDER — replace the BODY of this file with the real screen.
 * Keep the composable signature EXACTLY as-is (the NavGraph depends on it).
 * Container access: com.cinnetemple.app.core.di.LocalAppContainer.current
 */
@Composable
fun AdminMovieScreen(nav: NavController, movieId: String?) {
    PlaceholderScreen("Admin Movie")
}
