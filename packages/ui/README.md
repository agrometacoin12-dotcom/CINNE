# @cinnetemple/ui

Web design-system component library implementing the **Netflix-style + Liquid
Glass** language from [`docs/UI_DESIGN.md`](../../docs/UI_DESIGN.md).

Planned components (Phase 1 web): `GlassPanel`, `GlassNav`, `HeroBanner`,
`ContentRow`, `PosterCard`, `Button`, `TextField`, `Modal`, `Toast`.

> Scaffolded now; components land alongside the Phase 1 web app build.
